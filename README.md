                        1
The difference between getelements by id/class or queryselector is
getElementById-we can find ay element by their id
getElementByClass-we can find multiple element by their class.
queryselector/All-we can select using css element.

                         2  
I do to using createElement to create and insert a new element into the DOM.

                         3  
Event Bubbling is when an event starts from the target element and then  upward to its parent elements.
For example, if you click a button inside a div, the click event first runs on the button, then the div.

                         4
Event delegatioon is a techniqe to use a parent element and control the whole child element.
It’s useful because it improves performance and works for dynamically added elements.

                          5
preventDefault() - stops the browser default action (like form submit or link redirect).
stopPropagation() - stops the event from bubbling up to parent elements.                          
